age = [22, 19, 24, 25, 26, 24, 25, 24]

lst = list(age)
print(len(lst) - len(age))

#A string is a list of characters, a list is an unordered, indexed list of items, a tuple is an ordered indexed list of items and a set is an unordered unidexed list of unique items.

str = 'I am a teacher and I love to inspire and teach people. How many unique words have been used in the sentence?'

str = str.split(' ')
print(str)